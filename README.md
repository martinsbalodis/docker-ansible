# docker container with preinstalled ansible from ansible ppa repository.

Run:
```sh
docker run --rm -ti martinsbalodis/docker-ansible bash
```

Build:
```sh
docker build --no-cache --tag=martinsbalodis/docker-ansible .
```